{
    "name": "@sidebound/platform-browser-preview",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json --noEmit",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "check": "pnpm run typecheck"
    },
    "dependencies": {
        "@sidebound/engine": "workspace:*"
    },
    "devDependencies": {
        "typescript": "~6.0.2"
    }
}
