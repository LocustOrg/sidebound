{
    "name": "@sidebound/game",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --host 0.0.0.0",
        "build": "tsc -p tsconfig.json && vite build",
        "preview": "vite preview --host 0.0.0.0",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "check": "pnpm run typecheck"
    },
    "dependencies": {
        "@sidebound/engine": "workspace:*",
        "@sidebound/platform-browser-preview": "workspace:*"
    },
    "devDependencies": {
        "typescript": "~6.0.2",
        "vite": "^8.0.12"
    }
}
